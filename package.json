{
  "name": "switchboard",
  "version": "0.1.0",
  "private": true,
  "description": "Personal control plane for terminal coding agents across several machines",
  "workspaces": [
    "packages/host",
    "packages/web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:host": "npm run dev -w @switchboard/host",
    "dev:web": "npm run dev -w @switchboard/web",
    "build": "npm run build -w @switchboard/host && npm run build -w @switchboard/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test -w @switchboard/host",
    "test:all": "npm run jobs:check-runtime && npm test && npm run jobs:test",
    "jobs:check-runtime": "node packages/jobs/scripts/check-runtime.cjs",
    "jobs:setup": "npm --prefix packages/jobs ci",
    "jobs:build": "npm --prefix packages/jobs run build && npm --prefix packages/jobs-ui run build",
    "jobs:test": "npm --prefix packages/jobs test",
    "jobs:typecheck": "npm --prefix packages/jobs run typecheck",
    "jobs:start": "npm --prefix packages/jobs start"
  }
}
